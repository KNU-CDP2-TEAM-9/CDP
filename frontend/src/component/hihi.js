import React, { useState, useEffect } from "react";
import classes from "./hihi.module.css";
const chatM = (props) => {
  return (
    <div className={classes.wrapper}>
      <div className={classes.ulWrapper}></div>
    </div>
  );
};

export default chatM;
